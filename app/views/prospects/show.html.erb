<fieldset id="" class="">
	<legend><%= @prospect.company_name %></legend>
</fieldset>

<%= render @prospect.comments %>

<fieldset id="new_comment" class="">
	<legend>New comment</legend>
</fieldset>
<%= simple_form_for Comment.new do |f| %>
	<%= f.input :content, :input_html => { :rows => 10, :cols => 20, :class => "generic" } %>
	<%= f.hidden_field :commentable_id, :value => @prospect.id %>
	<%= f.hidden_field :commentable_type, :value => "Prospect" %>
  	<%= f.submit "Post comment", :class => "btn btn-success" %>
<% end %>

<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              prospects_path, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_prospect_path(@prospect), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              @prospect,
              :method => 'delete',
              :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')),
              :class => 'btn btn-danger' %>
</div>